# Nixpacks config for frontend (Vite static build, served via `vite preview`).
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "npm run preview"
